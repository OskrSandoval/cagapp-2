---
epic: 4
date: 09-24-2026 10:15
verdict: accepted-with-open-items
criteria: declared
headless: false
---

# Retrospectiva — Épica 4: Actividad y Comunidad

## Resumen de la épica

- **Stories:** 4.1 Ver mi actividad en el Perfil, 4.2 Ver calificaciones de otros usuarios en un baño — las 2 en `done`, `pending_stories: []` (confirmado con `sprint_status.py detect-epic --epic 4`). Esta es la última épica del roadmap (1-4 completo).
- **Rango de diff:** `11738713bb573b0ebf7991275c9f3ad7482f6cef..559f290` (4 commits: `02def24`, `10d106e`, `c2cae25`, `559f290`).
- **Criterios de aceptación:** declarados por story en `epics.md`, sin un criterio a nivel de épica.
- **Evidencia disponible:** las 2 specs, cada una con su `Review Triage Log` completo. `deferred-work.md` tiene 1 entrada de esta épica.
- **Evidencia faltante:** ninguna relevante — épica construida dentro de la conversación actual.

## Hallazgos

### Vista agregada — duplicación entre stories (causado directamente por esta épica)

`estrellasEstaticas` (renderiza una calificación 1-5 como `'★'.repeat(n) + '☆'.repeat(5 - n)`) está duplicada, línea por línea, entre `frontend/src/paginas/Perfil.jsx:4` (Story 4.1, nueva) y `frontend/src/paginas/Detalle.jsx:16` (ya existente desde las Épicas 2/3). Story 4.1 escribió su propia copia en vez de extraer/reusar la que ya vivía en `Detalle.jsx` — ninguna revisión lo pudo ver porque la revisión de 4.1 solo miró el diff de `Perfil.jsx`, nunca cruzó contra `Detalle.jsx`. Confirmado leyendo ambos archivos hoy: misma firma, misma implementación exacta. **Disposición: fix now** (trivial: mover a `frontend/src/paginas/calificacion.js`, que ya exporta `CAPTIONS_CALIFICACION`/`bandaCalificacion` para el mismo dominio, y que ambos archivos importen desde ahí).

### Vista agregada — `Detalle.jsx` sigue creciendo (ya trackeado en la retro de la Épica 3)

Story 4.2 le agregó 60 líneas netas más a `Detalle.jsx` (la sección "Lo que dice la gente") — mismo archivo ya señalado como componente de 4 responsabilidades en `epic-3-retro-2026-09-24.md`. No se repite el hallazgo aquí; ver esa retro para la propuesta de extracción (`FlujoCheckin`/`SelectorCalificacion`).

### Vista agregada — reconciliación spec-a-implementación

- `obtenerActividad` (Perfil) trae todo el historial de `checkins`/`baños`/`calificaciones` sin límite, y ningún `fetch` del frontend tiene timeout/`AbortController` — diferido explícitamente en `deferred-work.md` (spec-4-1), misma categoría de escala ya diferida desde 2.1/3.2 para la primera parte, patrón preexistente en todos los `api/*.js` para la segunda. **Disposición: defer, ya trackeado.**

### Revisión de las 2 stories (ya cubierta a nivel de story, no repetida aquí)

Ambas specs tienen su `Review Triage Log` con hallazgos ya verificados y parchados en la misma sesión (ej.: `aria-label` con el valor numérico de la calificación en vez de `aria-hidden` sin alternativa en `Perfil.jsx`, ordenamiento de `created_at` por `Date` en vez de string en `calificacionesService.js`). No se repite ese trabajo aquí.

## Verificación de comportamiento

- **Regresión automatizada (hoy, 2026-09-24):** `cd backend && npx vitest run test/perfilesService.test.js test/perfiles.routes.test.js test/calificacionesService.test.js test/calificaciones.routes.test.js` → 4 archivos, 65 tests, todos pasan. `cd frontend && npx vitest run test/Perfil.test.jsx test/perfilesApi.test.js test/Detalle.test.jsx` → 3 archivos, 45 tests, todos pasan.
- **Extremo a extremo:** el usuario probó Perfil (con y sin actividad) y "Lo que dice la gente" (dos cuentas calificando el mismo baño) en el navegador contra Supabase real antes de marcar cada story `done`. No se repite hoy.

## Seguimiento de retro previa

- **Épica 1** (`epic-1-retro-2026-09-24.md`): 2 action items. Sin evidencia de avance — ningún commit de esta épica toca `Login.jsx`. Sin cambio.
- **Épica 2** (`epic-2-retro-2026-09-24.md`): 1 action item (pila de overlays en `Mapa.jsx`). Sin evidencia de avance — `Mapa.jsx` solo ganó el wiring de `Perfil` (Story 4.1), que es exactamente el patrón que el action item pedía reconsiderar, repetido una tercera vez sin cambio. Sin cambio de estado, pero refuerza la lección.
- **Épica 3** (`epic-3-retro-2026-09-24.md`): 2 action items (extraer sub-componentes de `Detalle.jsx`; actualizar `deferred-work.md` sobre el índice de `checkins` ya resuelto). Sin evidencia de avance en ninguno de los dos — `deferred-work.md` no se editó desde esa retro. Sin cambio.

## Action Items

1. **[Fix now]** Extraer `estrellasEstaticas` de `Perfil.jsx`/`Detalle.jsx` a `frontend/src/paginas/calificacion.js` (ya comparte dominio con `CAPTIONS_CALIFICACION`/`bandaCalificacion`) y que ambos archivos importen desde ahí en vez de mantener implementaciones duplicadas. Owner: dev, parche pequeño y de bajo riesgo.
2. **[Deferred, ya trackeado — sin cambio]** `obtenerActividad` sin límite; `fetch` del frontend sin timeout. `deferred-work.md` (spec-4-1).
3. **[Recordatorio, no nuevo]** El action item de la Épica 2 sobre no repetir el patrón `{mostrando<X> && <X/>}` en `Mapa.jsx` sin evaluar una pila de overlays sigue sin atenderse, y esta épica lo repitió una vez más (`Perfil`). No se abre un item nuevo — se refuerza la prioridad del ya existente.

## Veredicto de aceptación

**accepted-with-open-items** (criterios declarados por story, no por épica). Con esta épica se completa el roadmap (Épicas 1-4, 10 stories).

- Las 2 stories están `done`, sin pendientes.
- Criterios de aceptación verificados en el navegador contra Supabase real al cerrar cada story, tests automatizados siguen pasando hoy.
- Ningún hallazgo bloqueante — la duplicación de `estrellasEstaticas` es trivial de corregir y no afecta el comportamiento observado por el usuario.
- Quedan 2 items abiertos de esta épica (1 fix trivial + 1 ya diferido), más el recordatorio de que 2 action items de épicas anteriores (Épicas 2 y 3) siguen sin atenderse.

## Preguntas abiertas

- Con el roadmap completo, ¿es momento de dedicar una sesión a los 3 action items de mantenibilidad acumulados (pila de overlays en `Mapa.jsx`, sub-componentes de `Detalle.jsx`, duplicación de `estrellasEstaticas`) antes de seguir agregando funcionalidad nueva, o se prioriza otra cosa?

## Supuestos

_No aplica — corrida interactiva; toda decisión fue confirmada por skr en el chat._
