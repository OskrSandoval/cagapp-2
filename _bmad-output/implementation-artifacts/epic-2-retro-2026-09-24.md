---
epic: 2
date: 09-24-2026 09:45
verdict: accepted-with-open-items
criteria: declared
headless: false
---

# Retrospectiva — Épica 2: Baños — Descubrir y Agregar

## Resumen de la épica

- **Stories:** 2.1 Ver baños cercanos en el mapa, 2.2 Alternar a vista de lista, 2.3 Ver el detalle de un baño, 2.4 Agregar un baño nuevo con búsqueda de duplicados — las 4 en `done`, `pending_stories: []` (confirmado con `sprint_status.py detect-epic --epic 2`).
- **Rango de diff:** `adc05ee57cd6d2eac9e10cc63efc30ef17fb1964..1b2ef27` (8 commits: `fd3d2ce`, `edb00e8`, `b561f6d`, `4967e5f`, `d46a575`, `1c2e22a`, `4143987`, `1b2ef27`). Mismo problema de atribución automática que en la Épica 1 (los subjects usan "(Story 2.N)", el script busca otro patrón); atribución manual por subject, sin ambigüedad.
- **Criterios de aceptación:** declarados por story en `epics.md`, sin un criterio a nivel de épica.
- **Evidencia disponible:** las 4 specs, cada una con su propio `Review Triage Log` completo (2-3 capas de revisión por story, corridas en esta misma sesión de conversación — session logs disponibles como el propio historial de esta conversación). `deferred-work.md` tiene 2 entradas de esta épica.
- **Evidencia faltante:** ninguna relevante — a diferencia de la Épica 1, esta épica sí se construyó dentro de la conversación actual, así que el "por qué" de cada decisión está en el propio historial, no solo en las specs.

## Hallazgos

### Vista agregada — crecimiento de `Mapa.jsx` en componente "todo-en-uno" (nadie lo vio por story)

`frontend/src/paginas/Mapa.jsx` pasó de 0 a 285 líneas netas a lo largo de 4 commits de esta épica (`git_evidence.py`: `added: 297, net: 285, commit_count: 4`), y hoy (con la Épica 4 encima) tiene 311 líneas. Ninguna revisión por story lo marcó porque cada historia agregó solo una pieza pequeña y justificada en su momento — pero la suma es un componente que hoy posee, todo junto: el ciclo de vida de Leaflet, la geolocalización (`watchPosition` + un fix puntual duplicado para reintentos), el fetch/estado de `banos`, la búsqueda por zona, el toggle mapa/lista, y el enrutamiento condicional de **tres** overlays a pantalla completa (`banoSeleccionado`/`Detalle` desde 2.3, `mostrandoCrearBano`/`CrearBano` desde 2.4, y `mostrandoPerfil`/`Perfil` que la Épica 4 agregó siguiendo el mismo patrón sin cuestionarlo). Las Stories 2.3 y 2.4 de esta épica son las que establecieron el patrón `{mostrando<X> && <X/>}` repetido tres veces sin una abstracción compartida ("pila de overlays" o similar). Hoy funciona y está bien cubierto por tests (`Mapa.test.jsx`, 384 líneas netas agregadas en la misma épica), pero un quinto overlay probablemente ya no debería copiar el mismo patrón sin revisarlo. **Disposición: lección de proceso**, no fix ahora (no hay bug activo, el riesgo es de mantenibilidad futura, no de corrección presente).

### Vista agregada — reconciliación spec-a-implementación (ya trackeado, sigue abierto)

- `GET /banos` sigue sin paginación ni límite (`select('*')` en `backend/src/servicios/banosService.js`) — diferido explícitamente desde Story 2.1 (`deferred-work.md`), nunca revisitado por 2.2/2.3/2.4 aunque cada una agregó más lectores de esa misma lista. Confirmado en el código actual: sin `range`/`limit` en `listarBanos`. **Disposición: defer, ya trackeado.**
- El radio de 1.5km de búsqueda de duplicados (Story 2.4) solo se aplica en el frontend contra el `banos` ya cargado — un `POST /banos` directo o una condición de carrera entre dos usuarios lo puede saltar. Diferido explícitamente en `deferred-work.md` de `spec-2-4`. Confirmado: `backend/src/controladores/banosController.js#postBano` no repite la validación de proximidad. **Disposición: defer, ya trackeado.**

### Revisión de las 4 stories (ya cubierta a nivel de story, no repetida aquí)

Cada spec de esta épica tiene su propio `Review Triage Log` con hallazgos de blind-hunter/edge-case-hunter/verification-gap ya verificados y con disposición (patch aplicado en la misma sesión, o rechazado con evidencia) al momento de construirse — no se repite ese trabajo aquí. Ejemplos de parches ya aplicados dentro de la épica: manejo de `fetch` sin try/catch en `banosApi.crearBano` (2.4), límite de 100 caracteres en campos de texto (2.4), `aria-pressed` inconsistente en el toggle (2.2), recentrado del mapa peleando con el pan manual del usuario (2.2). Ninguno de estos sigue abierto hoy.

## Verificación de comportamiento

- **Regresión automatizada (hoy, 2026-09-24):** `cd backend && npx vitest run test/banosService.test.js test/banos.routes.test.js` → 2 archivos, 23 tests, todos pasan. `cd frontend && npx vitest run test/Mapa.test.jsx test/Lista.test.jsx test/Detalle.test.jsx test/CrearBano.test.jsx test/banosApi.test.js` → 5 archivos, 81 tests, todos pasan — confirma que el contrato de comportamiento de la Épica 2 sigue intacto después de que las Épicas 3-4 se construyeron encima.
- **Extremo a extremo:** el usuario probó cada story en el navegador contra Supabase real durante esta misma conversación antes de marcarla `done` (agregar un baño real, alternar mapa/lista, ver el detalle) — no se repite hoy, se acepta como evidencia ya asentada en el propio historial de esta sesión.

## Seguimiento de retro previa

Existe una retro previa (Épica 1, `epic-1-retro-2026-09-24.md`, corrida minutos antes). Sus 2 action items nuevos (`epic-1-retro-item-1`/`epic-1-retro-item-2`) son de la Épica 1 (parche a `Login.jsx`, lección de proceso) y no tienen relación directa con código de la Épica 2 — no hay evidencia de que hayan avanzado (ningún commit de esta épica toca `Login.jsx`). Se registran como siguen: **sin evidencia de avance**, estado propuesto: sin cambio (siguen `open`).

## Action Items

1. **[Lección de proceso]** Antes de agregar un cuarto o quinto overlay a pantalla completa en `Mapa.jsx` (`{mostrando<X> && <X/>}`), evaluar extraer una pila de overlays compartida (un solo estado `overlayActivo` en vez de N booleanos independientes) — el patrón ya se repitió 3 veces sin cuestionarse. Sin owner de código; nota para la próxima historia que toque `Mapa.jsx`.
2. **[Deferred, ya trackeado — sin cambio]** `GET /banos` sin paginación/límite. `deferred-work.md` (spec-2-1).
3. **[Deferred, ya trackeado — sin cambio]** Búsqueda de duplicados de 1.5km solo validada en el frontend, no en el backend. `deferred-work.md` (spec-2-4).

## Veredicto de aceptación

**accepted-with-open-items** (criterios declarados por story, no por épica).

- Las 4 stories están `done`, sin stories pendientes.
- Cada criterio de aceptación fue verificado en el navegador contra Supabase real al cerrar su story (registrado en el historial de esta conversación) y los tests automatizados siguen pasando hoy sin regresión.
- Ningún hallazgo bloqueante — el crecimiento de `Mapa.jsx` es una nota de mantenibilidad, no un defecto activo, y los dos items de escala ya estaban diferidos a propósito desde su story de origen.
- Quedan 3 items abiertos (1 nuevo + 2 ya trackeados), de ahí "con items abiertos".

## Preguntas abiertas

- ¿Vale la pena refactorizar `Mapa.jsx` a una pila de overlays ahora, antes de que la Épica 3/4 (ya construidas) lo hagan crecer más, o esperar a que un quinto overlay lo justifique? (Decisión de priorización de skr, no bloquea nada hoy.)
- ¿La validación de 1.5km en el backend se retoma pronto, o se acepta el riesgo indefinidamente dado que hoy no hay tráfico concurrente real?

## Supuestos

_No aplica — corrida interactiva; toda decisión fue confirmada por skr en el chat._
